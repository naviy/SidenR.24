import { styled } from '@mui/material';
import { TransitionGroup as TransitionGroup_ } from 'react-transition-group';






export var TransitionGroup = styled(
	TransitionGroup_
)({
	position: "relative",
	flex: 1,
	display: "inherit",
	flexDirection: "inherit",
	gap: "inherit",
})
